import Image from 'next/image';import { ArrowUpRight } from 'lucide-react';
export function ImageCard({ project }){const h=project.size==='tall'?'h-[520px]':project.size==='wide'?'h-[350px]':'h-[420px]';return <div className={`group image-frame rounded-[30px] ${h} shadow-soft`}><Image src={project.image} alt={project.title} fill className="object-cover transition-transform duration-700 group-hover:scale-105"/><div className="absolute inset-x-0 bottom-0 z-10 p-6 text-white"><p className="text-xs uppercase tracking-[.22em] text-luxuryGold">{project.category}</p><div className="mt-2 flex items-end justify-between"><div><h3 className="text-xl font-medium">{project.title}</h3><p className="text-sm text-white/70">{project.location}</p></div><ArrowUpRight className="opacity-0 transition group-hover:opacity-100"/></div></div></div>}
